hazelcast:
  properties:
    hazelcast.logging.type: log4j2

  jet:
    enabled: True
    resource-upload-enabled: True

  vector-collection:
    patents:
      indexes:
        - name: semantic-search
          dimension: 384
          metric: COSINE
